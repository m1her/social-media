import Spinner from "@/Components/Spinner";

export default function Loading() {
    return (
      <div className="wrap-main flex flex-col items-center h-screen !border-0 relative">
       
      </div>
    );
  }
  